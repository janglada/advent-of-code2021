extern crate core;
use crate::shared::{Graph, NodeData, TreeNodeType};
use core::utils::donwload_puzzle;
use trees::{Node, Tree};

pub async fn solve_part1() {}

#[cfg(test)]
mod tests {
    use super::*;
    use std::cmp;
    use std::collections::HashMap;

    #[test]
    fn masks1() {}
}
